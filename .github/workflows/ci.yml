name: CI

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  sonarqube:
    name: Build, test and analyze with SonarQube Cloud
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Java (required by SonarScanner)
        uses: actions/setup-java@v4
        with:
          java-version: 17
          distribution: temurin

      - name: Set up .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.x'

      # Sonar steps are skipped when the token is unavailable (e.g. fork PRs)
      # so the build and tests still run.
      - name: Install dotnet-sonarscanner
        if: ${{ secrets.SONAR_TOKEN != '' }}
        run: dotnet tool install --global dotnet-sonarscanner

      - name: Begin SonarQube analysis
        if: ${{ secrets.SONAR_TOKEN != '' }}
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          dotnet sonarscanner begin \
            /k:"Tim-Pohlmann_ChannelDungeonsWebsite" \
            /o:"tim-pohlmann" \
            /d:sonar.host.url="https://sonarcloud.io" \
            /d:sonar.token="${SONAR_TOKEN}" \
            /d:sonar.cs.opencover.reportsPaths="TestResults/**/coverage.opencover.xml" \
            /d:sonar.coverage.exclusions="**/Program.cs,**/wwwroot/**" \
            /d:sonar.exclusions="**/wwwroot/_framework/**,**/obj/**,**/bin/**"

      - name: Build solution
        run: dotnet build ChannelDungeons.slnx --configuration Release

      - name: Run tests with coverage
        run: |
          dotnet test ChannelDungeons.Web.Tests/ChannelDungeons.Web.Tests.csproj \
            --configuration Release \
            --no-build \
            --collect:"XPlat Code Coverage" \
            --results-directory ./TestResults \
            -- DataCollectionRunSettings.DataCollectors.DataCollector.Configuration.Format=opencover

      - name: End SonarQube analysis
        if: ${{ secrets.SONAR_TOKEN != '' }}
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: dotnet sonarscanner end /d:sonar.token="${SONAR_TOKEN}"
